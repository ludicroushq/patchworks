name: setup
description: Install the pinned Mise toolchain and Bun dependencies

runs:
  using: composite
  steps:
    - uses: jdx/mise-action@dad1bfd3df957f44999b559dd69dc1671cb4e9ea # v4.2.1
    - run: bun install --frozen-lockfile
      shell: bash
